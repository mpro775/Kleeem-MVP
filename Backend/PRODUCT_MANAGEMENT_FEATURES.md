# نظام إدارة المنتجات المتقدم - Kleeem MVP

## 📋 نظرة عامة

تم تطوير نظام إدارة المنتجات ليشمل مجموعة شاملة من الميزات المتقدمة لتلبية احتياجات التجار، بما في ذلك إدارة المتغيرات، المنتجات الرقمية، النشر المؤجل، واستيراد/تصدير CSV.

---

## 🎯 الميزات المُضافة

### 1️⃣ نظام المتغيرات المتقدم (Product Variants)

يتيح للتجار إدارة منتجات بمتغيرات متعددة (ألوان، مقاسات، روائح، إلخ) مع خصائص مستقلة لكل متغير.

#### المزايا:
- ✅ **SKU فريد** لكل متغير مع التحقق التلقائي من الفرادة
- ✅ **Barcode اختياري** لكل متغير
- ✅ **خصائص مخصصة** (Attributes): مثل `{color: 'أحمر', size: 'L'}`
- ✅ **أسعار منفصلة** لكل متغير
- ✅ **إدارة مخزون** مستقلة لكل متغير مع عتبة المخزون المنخفض
- ✅ **صور متعددة** لكل متغير
- ✅ **الوزن** لكل متغير (للشحن)
- ✅ **حالة التوفر** المستقلة لكل متغير

#### الملفات الجديدة:
- `schemas/product-variant.schema.ts` - Schema المتغيرات
- `dto/product-variant.dto.ts` - DTOs للإنشاء والتحديث

#### مثال على الاستخدام:

```typescript
POST /products
{
  "name": "قميص رياضي",
  "price": 99.99,
  "hasVariants": true,
  "variants": [
    {
      "sku": "SHIRT-RED-L",
      "barcode": "1234567890123",
      "attributes": { "color": "أحمر", "size": "L" },
      "price": 99.99,
      "stock": 50,
      "lowStockThreshold": 10,
      "images": ["https://example.com/red-l.jpg"],
      "isAvailable": true,
      "weight": 250
    },
    {
      "sku": "SHIRT-BLUE-M",
      "attributes": { "color": "أزرق", "size": "M" },
      "price": 89.99,
      "stock": 30,
      "images": ["https://example.com/blue-m.jpg"]
    }
  ]
}
```

#### آلية حساب السعر:
عند وجود متغيرات، يتم حساب `priceEffective` تلقائياً من أقل سعر متغير متاح.

---

### 2️⃣ المنتجات الرقمية والخدمات

دعم كامل للمنتجات الرقمية (Digital Products) والخدمات مع مخزون لامحدود.

#### المزايا:
- ✅ ثلاثة أنواع: `physical` | `digital` | `service`
- ✅ معلومات الملف الرقمي (رابط التحميل، الحجم، الصيغة)
- ✅ مخزون غير محدود للمنتجات الرقمية والخدمات
- ✅ Validation تلقائي للمنتجات الرقمية

#### الحقول المُضافة:

```typescript
{
  "productType": "digital",
  "digitalAsset": {
    "downloadUrl": "https://example.com/download/ebook.pdf",
    "fileSize": 1024000,
    "format": "PDF"
  },
  "isUnlimitedStock": true
}
```

#### حالات الاستخدام:
- بطاقات شحن
- كتب إلكترونية
- برامج وتطبيقات
- دورات تدريبية
- خدمات استشارية

---

### 3️⃣ المسودة والنشر المؤجل

نظام متطور لإدارة حالات النشر مع دعم النشر التلقائي المؤجل.

#### حالات النشر:
- **draft** - مسودة (غير مرئي للعملاء)
- **published** - منشور (مرئي للعملاء)
- **scheduled** - مجدول للنشر (سيُنشر تلقائياً في الوقت المحدد)
- **archived** - مؤرشف

#### الحقول:
```typescript
{
  "status": "scheduled",
  "publishedAt": "2024-01-15T10:00:00Z",
  "scheduledPublishAt": "2024-01-20T08:00:00Z"
}
```

#### النشر التلقائي:
- ✅ **Cron Job** يعمل كل 5 دقائق
- ✅ يفحص المنتجات المجدولة التي حان وقت نشرها
- ✅ تحويل `status` من `scheduled` إلى `published`
- ✅ تسجيل `publishedAt` تلقائياً

#### الملف المُحدّث:
- `utils/products.cron.ts` - إضافة دالة `publishScheduledProducts()`

---

### 4️⃣ المنتجات الشبيهة (Related Products)

إمكانية ربط منتجات ذات صلة (حد أقصى 10 منتجات).

#### Endpoints الجديدة:

**تحديث المنتجات الشبيهة:**
```http
PUT /products/:id/related
{
  "relatedProductIds": [
    "507f1f77bcf86cd799439011",
    "507f191e810c19729de860ea"
  ]
}
```

**جلب المنتجات الشبيهة:**
```http
GET /products/:id/related
```
يرجع قائمة كاملة بالمنتجات الشبيهة (populated).

#### Validation:
- ✅ حد أقصى 10 منتجات
- ✅ التحقق من وجود المنتجات
- ✅ التحقق من أن المنتجات تابعة لنفس التاجر
- ✅ منع المنتج من الإشارة لنفسه

---

### 5️⃣ إدارة التاجز (Keywords/Tags)

نظام محسّن لإدارة الكلمات المفتاحية مع endpoint للـ autocomplete.

#### Endpoint جديد:
```http
GET /products/tags
```

**الاستجابة:**
```json
{
  "tags": ["إلكترونيات", "هواتف", "أجهزة لوحية", "اكسسوارات"],
  "count": 4
}
```

#### الاستخدام:
- Autocomplete عند إضافة منتج جديد
- عرض جميع التاجز المستخدمة
- تحسين تجربة المستخدم

---

### 6️⃣ استيراد/تصدير CSV

نظام شامل لاستيراد وتصدير المنتجات بصيغة CSV.

#### Endpoints:

**تصدير المنتجات:**
```http
GET /products/export/csv
```
يُصدّر جميع منتجات التاجر إلى CSV مع دعم المتغيرات.

**استيراد المنتجات:**
```http
POST /products/import/csv
Content-Type: multipart/form-data

file: [CSV file]
```

**الاستجابة:**
```json
{
  "success": 45,
  "failed": 5,
  "errors": [
    {
      "row": 12,
      "error": "Invalid price"
    }
  ]
}
```

**تنزيل Template:**
```http
GET /products/export/csv/template
```
يُرجع ملف CSV فارغ كمثال للاستيراد.

#### الأعمدة المدعومة:
- `name`, `description`, `price`, `currency`
- `sku`, `barcode`, `stock`
- `category`, `images`, `keywords`
- `productType`, `isAvailable`, `status`
- دعم متغيرات متعددة في ملف واحد

#### الملف الجديد:
- `services/product-csv.service.ts` - منطق الاستيراد/التصدير

---

### 7️⃣ تحسينات SEO للفئات

إضافة حقول SEO محسّنة للفئات.

#### الحقول الجديدة:
- **seoTitle** - عنوان SEO مخصص
- **seoDescription** - وصف SEO مخصص
- **icon** - أيقونة الفئة (منفصلة عن الصورة)

#### مثال:
```typescript
{
  "name": "إلكترونيات",
  "description": "جميع المنتجات الإلكترونية",
  "seoTitle": "أفضل الإلكترونيات - متجر كليم",
  "seoDescription": "تسوق أحدث المنتجات الإلكترونية بأفضل الأسعار",
  "image": "https://example.com/electronics.jpg",
  "icon": "https://example.com/icons/electronics.svg"
}
```

---

### 8️⃣ نظام Validation المتقدم

خدمة مركزية للتحقق من صحة البيانات.

#### الملف الجديد:
`services/product-validation.service.ts`

#### الوظائف:

**1. التحقق من SKU:**
```typescript
validateUniqueSku(merchantId, sku, excludeProductId?)
validateAllSkus(merchantId, skus[], excludeProductId?)
```

**2. التحقق من Barcode:**
```typescript
checkBarcodeUniqueness(merchantId, barcode, excludeProductId?)
```

**3. التحقق من المنتجات الرقمية:**
```typescript
validateDigitalProduct(productType, digitalAsset)
```

**4. التحقق من النشر المؤجل:**
```typescript
validateScheduledPublish(status, scheduledPublishAt)
```

**5. التحقق من المنتجات الشبيهة:**
```typescript
validateRelatedProducts(merchantId, relatedProductIds, currentProductId?)
```

**6. التحقق من المتغيرات:**
```typescript
validateVariants(hasVariants, variants)
```

---

### 9️⃣ فهارس MongoDB محسّنة

إضافة فهارس لتحسين الأداء.

#### الفهارس الجديدة:

**للمتغيرات:**
```javascript
// SKU فريد
{ merchantId: 1, 'variants.sku': 1 } // unique, sparse

// Barcode
{ merchantId: 1, 'variants.barcode': 1 } // sparse
```

**للنشر:**
```javascript
// حالة النشر
{ merchantId: 1, status: 1, publishedAt: -1, createdAt: -1, _id: -1 }

// النشر المؤجل
{ status: 1, scheduledPublishAt: 1 } // partialFilter: status = 'scheduled'
```

**لنوع المنتج:**
```javascript
{ merchantId: 1, productType: 1, status: 1, createdAt: -1, _id: -1 }
{ merchantId: 1, productType: 1, isUnlimitedStock: 1 }
```

---

## 📁 هيكل الملفات

### الملفات الجديدة:
```
Backend/src/modules/products/
├── schemas/
│   └── product-variant.schema.ts          [جديد]
├── dto/
│   └── product-variant.dto.ts             [جديد]
└── services/
    ├── product-validation.service.ts      [جديد]
    └── product-csv.service.ts             [جديد]
```

### الملفات المُحدّثة:
```
Backend/src/modules/products/
├── schemas/
│   └── product.schema.ts                  [محدّث]
├── dto/
│   ├── create-product.dto.ts              [محدّث]
│   └── product-response.dto.ts            [محدّث]
├── services/
│   ├── product-commands.service.ts        [محدّث]
│   ├── product-queries.service.ts         [محدّث]
│   └── product-public.service.ts          [محدّث]
├── repositories/
│   └── mongo-products.repository.ts       [محدّث]
├── utils/
│   └── products.cron.ts                   [محدّث]
├── products.controller.ts                 [محدّث]
├── products.service.ts                    [محدّث]
└── products.module.ts                     [محدّث]

Backend/src/modules/categories/
├── schemas/
│   └── category.schema.ts                 [محدّث]
├── dto/
│   └── create-category.dto.ts             [محدّث]
└── categories.service.ts                  [محدّث]
```

---

## 🔧 التبعيات المطلوبة

أضف هذه الحزم إلى `package.json`:

```bash
npm install csv-parse csv-stringify
```

---

## 🚀 API Endpoints الجديدة

### المنتجات:

| Method | Endpoint | الوصف |
|--------|----------|-------|
| `PUT` | `/products/:id/related` | تحديث المنتجات الشبيهة |
| `GET` | `/products/:id/related` | جلب المنتجات الشبيهة |
| `GET` | `/products/tags` | جلب جميع التاجز |
| `GET` | `/products/export/csv` | تصدير المنتجات إلى CSV |
| `POST` | `/products/import/csv` | استيراد المنتجات من CSV |
| `GET` | `/products/export/csv/template` | تنزيل template CSV |

---

## 📊 Schema Changes

### Product Schema - الحقول الجديدة:

```typescript
{
  // المتغيرات
  variants: ProductVariant[]
  hasVariants: boolean

  // نوع المنتج
  productType: 'physical' | 'digital' | 'service'
  digitalAsset?: {
    downloadUrl: string
    fileSize?: number
    format?: string
  }
  isUnlimitedStock: boolean

  // النشر
  status: 'draft' | 'published' | 'scheduled' | 'archived'
  publishedAt?: Date
  scheduledPublishAt?: Date

  // المنتجات الشبيهة
  relatedProducts: Types.ObjectId[]
}
```

### ProductVariant Schema:

```typescript
{
  sku: string              // مطلوب، فريد
  barcode?: string         // اختياري
  attributes: Record<string, string>  // مثل {color: 'أحمر', size: 'L'}
  price: number            // مطلوب
  stock: number            // مطلوب
  lowStockThreshold?: number
  images: string[]
  isAvailable: boolean
  weight?: number
}
```

### Category Schema - الحقول الجديدة:

```typescript
{
  seoTitle: string
  seoDescription: string
  icon: string
}
```

---

## 🔄 سير العمل (Workflows)

### 1. إضافة منتج بمتغيرات:

```
1. المستخدم يُنشئ منتج مع hasVariants: true
2. النظام يتحقق من فرادة جميع SKUs
3. النظام يحفظ المنتج مع المتغيرات
4. يتم حساب priceEffective تلقائياً
5. الفهرسة في قاعدة البيانات
```

### 2. النشر المؤجل:

```
1. المستخدم يُنشئ منتج مع status: 'scheduled'
2. يحدد scheduledPublishAt
3. Cron job يفحص كل 5 دقائق
4. عند الوقت المحدد:
   - تحويل status إلى 'published'
   - تسجيل publishedAt
   - إزالة scheduledPublishAt
```

### 3. استيراد CSV:

```
1. المستخدم يرفع ملف CSV
2. النظام يقوم بـ parse الملف
3. التحقق من كل صف:
   - Validation للحقول المطلوبة
   - فحص SKU uniqueness
   - التحقق من الأسعار والأرقام
4. إنشاء/تحديث المنتجات
5. إرجاع تقرير (success, failed, errors)
```

---

## ⚙️ الإعدادات والتكوين

### Environment Variables:

لا توجد متغيرات بيئية جديدة مطلوبة. النظام يستخدم الإعدادات الحالية.

### Cron Jobs:

```typescript
// في products.cron.ts
@Cron(CronExpression.EVERY_5_MINUTES)
publishScheduledProducts()

@Cron(CronExpression.EVERY_10_MINUTES)
disableExpiredOffers()
```

---

## 🧪 أمثلة على الاستخدام

### مثال 1: منتج رقمي

```http
POST /products
{
  "name": "كتاب إلكتروني - تعلم البرمجة",
  "description": "كتاب شامل لتعلم البرمجة",
  "price": 49.99,
  "productType": "digital",
  "digitalAsset": {
    "downloadUrl": "https://storage.example.com/books/programming-101.pdf",
    "fileSize": 5242880,
    "format": "PDF"
  },
  "isUnlimitedStock": true,
  "status": "published",
  "keywords": ["برمجة", "تعليم", "كتب"]
}
```

### مثال 2: منتج بمتغيرات

```http
POST /products
{
  "name": "حذاء رياضي",
  "description": "حذاء رياضي عالي الجودة",
  "price": 199.99,
  "hasVariants": true,
  "variants": [
    {
      "sku": "SHOE-41-BLACK",
      "barcode": "6281234567890",
      "attributes": {
        "size": "41",
        "color": "أسود"
      },
      "price": 199.99,
      "stock": 25,
      "lowStockThreshold": 5,
      "images": ["https://example.com/shoe-41-black.jpg"]
    },
    {
      "sku": "SHOE-42-WHITE",
      "attributes": {
        "size": "42",
        "color": "أبيض"
      },
      "price": 219.99,
      "stock": 15
    }
  ],
  "category": "507f1f77bcf86cd799439011",
  "keywords": ["أحذية", "رياضة", "نايكي"]
}
```

### مثال 3: نشر مؤجل

```http
POST /products
{
  "name": "عرض الجمعة السوداء",
  "price": 99.99,
  "status": "scheduled",
  "scheduledPublishAt": "2024-11-29T00:00:00Z"
}
```

---

## 📈 الفوائد والتحسينات

### للتجار:
- ✅ إدارة متقدمة للمخزون على مستوى المتغيرات
- ✅ دعم المنتجات الرقمية والخدمات
- ✅ نشر مجدول للحملات التسويقية
- ✅ استيراد/تصدير جماعي للمنتجات
- ✅ ربط المنتجات الشبيهة لزيادة المبيعات

### للعملاء:
- ✅ تجربة أفضل مع اختيار المتغيرات
- ✅ معلومات دقيقة عن المخزون
- ✅ اقتراحات منتجات شبيهة
- ✅ SEO محسّن لمحركات البحث

### للنظام:
- ✅ أداء محسّن مع فهارس جديدة
- ✅ Validation شامل للبيانات
- ✅ معمارية قابلة للتوسع
- ✅ كود نظيف ومنظم

---

## 🔐 الأمان والتحقق

### Validations المطبقة:

1. **SKU Uniqueness** - فحص فرادة SKU داخل التاجر
2. **Data Integrity** - التحقق من صحة البيانات
3. **Merchant Isolation** - عزل بيانات التجار
4. **File Upload Security** - التحقق من ملفات CSV
5. **Related Products** - منع الإشارات الدائرية

---

## 📝 ملاحظات مهمة

### عند استخدام المتغيرات:
- يجب تعيين `hasVariants: true`
- SKU يجب أن يكون فريد على مستوى التاجر
- السعر الأساسي (`price`) يُحسب تلقائياً من أقل متغير متاح

### عند استخدام النشر المؤجل:
- التأكد من أن `scheduledPublishAt` في المستقبل
- Cron job يعمل كل 5 دقائق (دقة النشر ±5 دقائق)

### عند استيراد CSV:
- التأكد من صحة التنسيق
- استخدام Template للحصول على البنية الصحيحة
- مراجعة تقرير الأخطاء بعد الاستيراد

---

## 🎓 الخطوات التالية (Future Enhancements)

### مقترحات للتطوير المستقبلي:
1. **Bulk Operations** - تحديث/حذف جماعي للمنتجات
2. **Advanced Filtering** - فلترة متقدمة حسب المتغيرات
3. **Inventory Alerts** - تنبيهات المخزون المنخفض
4. **Price History** - تتبع تاريخ الأسعار
5. **Variant Images Auto-sync** - مزامنة تلقائية للصور
6. **Multi-language Support** - دعم متعدد اللغات للمتغيرات
7. **Advanced Analytics** - تحليلات مبيعات المتغيرات

---

## 📞 الدعم

في حال وجود أي استفسارات أو مشاكل:
- راجع هذا الملف للتوضيحات
- افحص ملفات الـ DTOs للتحقق من البنية المطلوبة
- استخدم Swagger documentation للـ API

---

**تاريخ التحديث:** نوفمبر 2024  
**الإصدار:** MVP v2.0  
**حالة التطوير:** ✅ مكتمل وجاهز للإنتاج

